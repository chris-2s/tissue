import {ConfigProperties} from "./index";

const config: ConfigProperties = {
    BASE_API: document.location.origin + '/api',
    BASE_IMAGE: document.location.origin + '/api'
}

export default config
